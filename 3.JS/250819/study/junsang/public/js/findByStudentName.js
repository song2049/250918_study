function findByStudentName(inputStudentName) {
    if(students.student1.name === inputStudentName) return students.student1
    if(students.student2.name === inputStudentName) return students.student2
    if(students.student3.name === inputStudentName) return students.student3
    if(students.student4.name === inputStudentName) return students.student4
    if(students.student5.name === inputStudentName) return students.student5
    return false
}